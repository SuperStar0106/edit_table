import { StyledComponentProps, styled } from '@mui/material';

type ChartViewStyleProps = StyledComponentProps & {};

export const ChartViewStyle = styled('div')<ChartViewStyleProps>(
    ({ theme }) => {
        return {};
    }
);